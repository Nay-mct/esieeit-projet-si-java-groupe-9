spring:
  datasource:
    url: jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3307}/${DB_NAME:project_si_db}?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=Europe/Paris
    username: ${DB_USER:project_user}
    password: ${DB_PASSWORD:project_pass}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
